// The only line you need to change after deploying the Worker.
// `wrangler deploy` prints this URL (also visible in the Cloudflare
// dashboard under Workers & Pages). Do not include a trailing slash.
window.STARS_API_BASE_URL = "https://stars-api.patrick-j-oneill.workers.dev";
